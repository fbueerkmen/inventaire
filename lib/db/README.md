# Requêtes base de données

Toutes les lectures / écritures Supabase (ou RPC) doivent être encapsulées ici, pas dans les composants UI.

Point d’entrée : **`index.ts`**. Voir **`lib/supabase/README.md`** pour les clients navigateur / serveur et la génération des types.
