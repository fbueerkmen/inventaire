# Copier vers .env.local et renseigner les valeurs (projet Supabase, région **Frankfurt**).

# URL du projet (Settings → API).
NEXT_PUBLIC_SUPABASE_URL=https://amyljtzbtmegthoehezr.supabase.co

# Clé **anon** / **publishable** (jamais la service_role côté client).
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optionnel — **uniquement** Route Handlers / scripts serveur, jamais exposée au navigateur.
#SUPABASE_SERVICE_ROLE_KEY=
